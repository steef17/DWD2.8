{% extends 'base.html.twig' %}

{% block body %}
    <h1>Parameter list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Rule</th>
                <th>Isdeleted</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for parameter in parameters %}
            <tr>
                <td><a href="{{ path('parameter_show', { 'id': parameter.id }) }}">{{ parameter.id }}</a></td>
                <td>{{ parameter.rule }}</td>
                <td>{% if parameter.isDeleted %}Yes{% else %}No{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('parameter_show', { 'id': parameter.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('parameter_edit', { 'id': parameter.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('parameter_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
